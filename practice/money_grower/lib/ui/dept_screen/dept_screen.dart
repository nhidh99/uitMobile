import 'package:flutter/material.dart';

class DeptScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Dept Screen")
    );
  }
}